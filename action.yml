name: "Custom Action"
description: "A simple JavaScript action"
inputs:
  currents-api-url:
    description: "Currents API URL"
    required: true
    default: "https://api.currents.dev/api/v1"
  run-id:
    description: "Cypress run id"
    required: true
  bearer-token:
    description: "Bearer authentication token"
    required: true
runs:
  using: "node16"
  main: "dist/index.js"
